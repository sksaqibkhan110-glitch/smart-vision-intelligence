version: '3.8'

services:
  surveillance-engine:
    build: .
    container_name: vision_analytics_service
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped